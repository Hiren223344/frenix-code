---
title: Windows (WSL)
description: Запускайте frenixcode в Windows через WSL.
---

import { Steps } from "@astrojs/starlight/components"

frenixcode можно запускать напрямую в Windows, но для лучшего опыта мы рекомендуем [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install). WSL дает Linux-среду, которая отлично работает с возможностями frenixcode.

:::tip[Почему WSL?]
WSL дает более высокую производительность файловой системы, полноценную поддержку терминала и совместимость с инструментами разработки, на которые опирается frenixcode.
:::

---

## Настройка

<Steps>

1. **Установите WSL**

   Если вы еще не сделали этого, установите WSL по [официальному руководству Microsoft](https://learn.microsoft.com/en-us/windows/wsl/install).

2. **Установите frenixcode в WSL**

   После настройки WSL откройте терминал WSL и установите frenixcode одним из [способов установки](/docs/).

   ```bash
   curl -fsSL https://frenixcode.dev/install | bash
   ```

3. **Запускайте frenixcode из WSL**

   Перейдите в каталог проекта (к файлам Windows можно обращаться через `/mnt/c/`, `/mnt/d/` и т.д.) и запустите frenixcode.

   ```bash
   cd /mnt/c/Users/YourName/project
   frenixcode
   ```

</Steps>

---

## Десктопное приложение + сервер в WSL

Если вы предпочитаете frenixcode Desktop, но хотите запускать сервер в WSL:

1. **Запустите сервер в WSL** с параметром `--hostname 0.0.0.0`, чтобы разрешить внешние подключения:

   ```bash
   frenixcode serve --hostname 0.0.0.0 --port 4096
   ```

2. **Подключите десктопное приложение** к `http://localhost:4096`

:::note
Если в вашей конфигурации `localhost` не работает, используйте IP-адрес WSL (выполните в WSL: `hostname -I`) и подключайтесь по `http://<wsl-ip>:4096`.
:::

:::caution
При использовании `--hostname 0.0.0.0` задайте `FRENIXCODE_SERVER_PASSWORD`, чтобы защитить сервер.

```bash
FRENIXCODE_SERVER_PASSWORD=your-password frenixcode serve --hostname 0.0.0.0
```

:::

---

## Веб-клиент + WSL

Для лучшего веб-опыта в Windows:

1. **Запускайте `frenixcode web` в терминале WSL**, а не в PowerShell:

   ```bash
   frenixcode web --hostname 0.0.0.0
   ```

2. **Открывайте в браузере Windows** адрес `http://localhost:<port>` (frenixcode выведет URL)

Запуск `frenixcode web` из WSL обеспечивает корректный доступ к файловой системе и интеграцию с терминалом, при этом интерфейс остается доступным из браузера Windows.

---

## Доступ к файлам Windows

WSL может получать доступ ко всем вашим файлам Windows через каталог `/mnt/`:

- `C:` drive → `/mnt/c/`
- `D:` drive → `/mnt/d/`
- И так далее

Пример:

```bash
cd /mnt/c/Users/YourName/Documents/project
frenixcode
```

:::tip
Для максимально плавной работы стоит клонировать или скопировать репозиторий в файловую систему WSL (например, в `~/code/`) и запускать frenixcode оттуда.
:::

---

## Советы

- Даже для проектов на дисках Windows запускайте frenixcode в WSL, чтобы получить более плавный доступ к файлам
- Используйте frenixcode вместе с [расширением WSL для VS Code](https://code.visualstudio.com/docs/remote/wsl) для единого рабочего процесса
- Конфигурация и сессии frenixcode хранятся в среде WSL по пути `~/.local/share/frenixcode/`

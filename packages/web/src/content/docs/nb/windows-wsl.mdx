---
title: Windows (WSL)
description: Kjør FrenixCode på Windows med WSL for best opplevelse.
---

import { Steps } from "@astrojs/starlight/components"

Selv om FrenixCode kan kjøre direkte på Windows, anbefaler vi å bruke [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install) for best mulig opplevelse. WSL gir et Linux-miljø som fungerer godt med funksjonene i FrenixCode.

:::tip[Hvorfor WSL?]
WSL gir bedre filsystemytelse, full terminalstøtte og kompatibilitet med utviklingsverktøyene FrenixCode er avhengig av.
:::

---

## Oppsett

<Steps>

1. **Installer WSL**

   Hvis du ikke har gjort det ennå, [installer WSL](https://learn.microsoft.com/en-us/windows/wsl/install) med den offisielle guiden fra Microsoft.

2. **Installer FrenixCode i WSL**

   Når WSL er satt opp, åpner du WSL-terminalen og installerer FrenixCode med en av [installasjonsmetodene](/docs/).

   ```bash
   curl -fsSL https://frenixcode.dev/install | bash
   ```

3. **Bruk FrenixCode fra WSL**

   Gå til prosjektmappen din (Windows-filer er tilgjengelige via `/mnt/c/`, `/mnt/d/` osv.) og kjør FrenixCode.

   ```bash
   cd /mnt/c/Users/YourName/project
   frenixcode
   ```

</Steps>

---

## Skrivebordsapp + WSL-server

Hvis du foretrekker FrenixCode-skrivebordsappen, men vil kjøre serveren i WSL:

1. **Start serveren i WSL** med `--hostname 0.0.0.0` for å tillate eksterne tilkoblinger:

   ```bash
   frenixcode serve --hostname 0.0.0.0 --port 4096
   ```

2. **Koble skrivebordsappen** til `http://localhost:4096`

:::note
Hvis `localhost` ikke fungerer i oppsettet ditt, bruk WSL-IP-adressen i stedet (fra WSL: `hostname -I`) og koble til `http://<wsl-ip>:4096`.
:::

:::caution
Når du bruker `--hostname 0.0.0.0`, sett `FRENIXCODE_SERVER_PASSWORD` for å sikre serveren.

```bash
FRENIXCODE_SERVER_PASSWORD=your-password frenixcode serve --hostname 0.0.0.0
```

:::

---

## Web-klient + WSL

For best web-opplevelse på Windows:

1. **Kjør `frenixcode web` i WSL-terminalen** i stedet for PowerShell:

   ```bash
   frenixcode web --hostname 0.0.0.0
   ```

2. **Åpne i Windows-nettleseren** på `http://localhost:<port>` (FrenixCode skriver ut URL-en)

Når du kjører `frenixcode web` fra WSL, får du riktig filsystemtilgang og terminalintegrasjon, samtidig som det er tilgjengelig fra Windows-nettleseren din.

---

## Tilgang til Windows-filer

WSL kan få tilgang til alle Windows-filer via `/mnt/`-katalogen:

- `C:`-stasjon → `/mnt/c/`
- `D:`-stasjon → `/mnt/d/`
- Og så videre...

Eksempel:

```bash
cd /mnt/c/Users/YourName/Documents/project
frenixcode
```

:::tip
For en jevnere opplevelse kan du klone/kopiere repoet ditt inn i WSL-filsystemet (for eksempel under `~/code/`) og kjøre FrenixCode der.
:::

---

## Tips

- Kjør FrenixCode i WSL for prosjekter lagret på Windows-stasjoner: filtilgang fungerer smidig
- Bruk [WSL-utvidelsen i VS Code](https://code.visualstudio.com/docs/remote/wsl) sammen med FrenixCode for en integrert utviklingsflyt
- FrenixCode-konfigurasjon og sesjoner lagres i WSL-miljøet på `~/.local/share/frenixcode/`
